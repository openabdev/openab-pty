name: nightly images

# Daily builds carrying the *latest* coding CLI for each variant — issue #12.
#
# The release channels cannot do this: the CLI version is an ARG pinned inside
# the openab base's own Dockerfile, so rebuilding FROM that base republishes the
# same CLI however often it runs. This lane starts from the same base (pulled,
# never modified — nothing here pushes, retags or deletes anything under
# ghcr.io/openabdev/openab) and overwrites the agent CLI with the version the
# vendor published most recently.
#
# Channel contract, deliberately different from image.yml:
#   - freshness over cohort atomicity: each variant publishes independently, so
#     one vendor breaking their latest does not hold back the rest. A failed
#     variant keeps yesterday's tag; the red job is the alert.
#   - nightly-<variant> is the moving tag; the
#     nightly-YYYYMMDD-<run-id>-<variant> tag is the immutable rollback point.
#   - pre-beta-* / beta-* and their build path are untouched.
#
# Every agent-CLI variant is covered. `native` is intentionally absent because
# it carries no coding CLI by definition, so there is nothing to update.

on:
  schedule:
    # Off-peak, and offset from the hour to dodge the scheduler rush.
    - cron: '23 6 * * *'
  workflow_dispatch:
  pull_request:
    paths:
      - Dockerfile.nightly
      - .github/workflows/nightly-images.yml

permissions:
  contents: read

concurrency:
  group: nightly-images
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: openabdev/openab-pty

jobs:
  # Same gate as image.yml, same reason: the image is built from whatever the
  # tests ran against. main was green at push time, but this runs up to a day
  # later on a runner image that has moved — re-proving is cheap, debugging a
  # published artifact is not.
  ci:
    uses: ./.github/workflows/ci.yml

  # One build per (variant, arch) on that arch's *native* GitHub-hosted runner —
  # ubuntu-24.04 for amd64, ubuntu-24.04-arm for arm64 (free for public repos).
  # Native means no cross toolchain, no QEMU, and — the real win — the smoke
  # test runs the arm64 image on real arm64 hardware instead of skipping it.
  #
  # Each leg builds and pushes *by digest* only (no tag). The merge job below
  # assembles the per-arch digests into the moving/immutable tags, so a tag
  # never points at a half-built manifest.
  build:
    needs: ci
    strategy:
      fail-fast: false
      matrix:
        variant: [antigravity, claude, codex, copilot, cursor, devin, gemini,
                  grok, hermes, kiro, mimocode, opencode, pi]
        arch: [amd64, arm64]
        include:
          - arch: amd64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
        # grok's vendor ships no arm64 Linux binary (issue #21), so it is
        # amd64-only; arm64 hosts fall back to Rosetta for grok alone.
        exclude:
          - variant: grok
            arch: arm64
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    outputs:
      # Not consumable directly (matrix outputs collapse), so the merge job
      # reads the per-arch digests from artifacts instead. Kept here for clarity.
      date: ${{ steps.meta.outputs.date }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Resolve the latest CLI version and the base digest
        id: meta
        env:
          # Used only for the Hermes cross-repo Contents API request. An
          # authenticated API fetch avoids the shared-runner 429s seen on
          # raw.githubusercontent.com (run 33565582163), while the resolved
          # commit below keeps the installer and checkout immutable.
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          echo "date=$(date -u +%Y%m%d)" >> "$GITHUB_OUTPUT"

          # The vendor's latest, resolved fresh each morning. Each mechanism
          # below was verified against the live endpoint before being wired in:
          #   npm registry /latest document ....... claude codex copilot gemini
          #                                         opencode mimocode pi
          #   GitHub releases/latest .............. antigravity hermes
          #   install script's embedded version ... cursor
          #   cli/current/manifest.json ........... devin (also carries sha256)
          #   GCS channel version file ............ grok
          #   vendor `latest` download URL ........ kiro (version learned from
          #                                         the built image afterwards)
          CLI_SHA256=""
          CLI_REF=""
          # Dockerfile.nightly mounts this build-context file without copying it
          # into the final image. Non-Hermes jobs still need the path to exist.
          printf '#!/bin/sh\nexit 1\n' > .nightly-hermes-install.sh

          github_api() {
            curl -fsSL --retry 3 --retry-all-errors \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H 'X-GitHub-Api-Version: 2022-11-28' "$@"
          }

          case "${{ matrix.variant }}" in
            claude)
              CLI_VERSION=$(curl -fsSL --retry 3 https://registry.npmjs.org/@anthropic-ai/claude-code/latest | jq -r .version) ;;
            codex)
              CLI_VERSION=$(curl -fsSL --retry 3 https://registry.npmjs.org/@openai/codex/latest | jq -r .version) ;;
            copilot)
              CLI_VERSION=$(curl -fsSL --retry 3 https://registry.npmjs.org/@github/copilot/latest | jq -r .version) ;;
            gemini)
              CLI_VERSION=$(curl -fsSL --retry 3 https://registry.npmjs.org/@google/gemini-cli/latest | jq -r .version) ;;
            opencode)
              CLI_VERSION=$(curl -fsSL --retry 3 https://registry.npmjs.org/opencode-ai/latest | jq -r .version) ;;
            mimocode)
              CLI_VERSION=$(curl -fsSL --retry 3 https://registry.npmjs.org/@mimo-ai/cli/latest | jq -r .version) ;;
            pi)
              CLI_VERSION=$(curl -fsSL --retry 3 https://registry.npmjs.org/@earendil-works/pi-coding-agent/latest | jq -r .version) ;;
            antigravity)
              CLI_VERSION=$(curl -fsSL --retry 3 https://api.github.com/repos/google-antigravity/antigravity-cli/releases/latest | jq -r .tag_name) ;;
            hermes)
              RELEASE=$(github_api https://api.github.com/repos/NousResearch/hermes-agent/releases/latest)
              CLI_VERSION=$(echo "$RELEASE" | jq -r .tag_name)

              # Dereference an annotated (possibly nested) release tag to its
              # immutable commit. Fetch both the installer and source checkout
              # from this SHA, never from the movable tag name.
              REF=$(github_api "https://api.github.com/repos/NousResearch/hermes-agent/git/ref/tags/${CLI_VERSION}")
              REF_TYPE=$(echo "$REF" | jq -r .object.type)
              CLI_REF=$(echo "$REF" | jq -r .object.sha)
              for _ in 1 2 3; do
                [ "$REF_TYPE" = "tag" ] || break
                REF=$(github_api "https://api.github.com/repos/NousResearch/hermes-agent/git/tags/${CLI_REF}")
                REF_TYPE=$(echo "$REF" | jq -r .object.type)
                CLI_REF=$(echo "$REF" | jq -r .object.sha)
              done
              [ "$REF_TYPE" = "commit" ] || {
                echo "FAIL: Hermes release $CLI_VERSION did not resolve to a commit" >&2
                exit 1
              }

              github_api -H 'Accept: application/vnd.github.raw+json' \
                "https://api.github.com/repos/NousResearch/hermes-agent/contents/scripts/install.sh?ref=${CLI_REF}" \
                > .nightly-hermes-install.sh
              [ "$(head -1 .nightly-hermes-install.sh)" = '#!/bin/bash' ] || {
                echo "FAIL: Hermes installer response is not the expected script" >&2
                exit 1
              }
              chmod 755 .nightly-hermes-install.sh ;;
            cursor)
              # The install script embeds the version it would install; there is
              # no separate version index. Anchored to the download URL shape so
              # an unrelated mention of the domain cannot match.
              CLI_VERSION=$(curl -fsSL --retry 3 https://cursor.com/install \
                | grep -oE 'downloads\.cursor\.com/lab/[^/]+/' | head -1 \
                | sed 's|downloads.cursor.com/lab/||; s|/$||') ;;
            devin)
              MANIFEST=$(curl -fsSL --retry 3 https://static.devin.ai/cli/current/manifest.json)
              CLI_VERSION=$(echo "$MANIFEST" | jq -r .version)
              # Per-arch sha256: the Dockerfile downloads the arch matching this
              # leg, so verify against that arch's checksum, not a fixed one.
              case "${{ matrix.arch }}" in
                amd64) DEVIN_KEY="x86_64-unknown-linux" ;;
                arm64) DEVIN_KEY="aarch64-unknown-linux" ;;
              esac
              CLI_SHA256=$(echo "$MANIFEST" | jq -r ".platforms[\"${DEVIN_KEY}\"].sha256") ;;
            grok)
              CLI_VERSION=$(curl -fsSL --retry 3 https://storage.googleapis.com/grok-build-public-artifacts/cli/stable | tr -d '[:space:]') ;;
            kiro)
              CLI_VERSION=latest ;;
          esac
          case "$CLI_VERSION" in
            ''|null|*[!A-Za-z0-9._+-]*)
              echo "FAIL: invalid CLI version for ${{ matrix.variant }}: $CLI_VERSION" >&2
              exit 1 ;;
          esac
          if [ -n "$CLI_REF" ] && ! echo "$CLI_REF" | grep -qE '^[0-9a-f]{40}$'; then
            echo "FAIL: invalid immutable ref for ${{ matrix.variant }}: $CLI_REF" >&2
            exit 1
          fi
          {
            echo "cli_version=$CLI_VERSION"
            echo "cli_sha256=$CLI_SHA256"
            echo "cli_ref=$CLI_REF"
          } >> "$GITHUB_OUTPUT"
          echo "Resolved ${{ matrix.variant }} CLI -> $CLI_VERSION${CLI_REF:+ @ $CLI_REF}"

          # The base is the same pre-beta variant the release channel builds
          # from. Pin the *index* (manifest-list) digest, not one arch's
          # sub-manifest: passing the index digest to FROM lets buildx pick the
          # sub-manifest matching this leg's native arch. Digest-pinning still
          # holds — the index digest is immutable and its members are fixed.
          T=$(curl -s "https://ghcr.io/token?scope=repository:openabdev/openab:pull&service=ghcr.io" | jq -r .token)
          BASE_TAG="pre-beta-${{ matrix.variant }}"
          DIGEST=$(curl -sI -H "Authorization: Bearer $T" \
            -H 'Accept: application/vnd.oci.image.index.v1+json,application/vnd.docker.distribution.manifest.list.v2+json' \
            "https://ghcr.io/v2/openabdev/openab/manifests/${BASE_TAG}" \
            | tr -d '\r' | awk -F': ' 'tolower($1)=="docker-content-digest"{print $2}')
          if [ -z "$DIGEST" ] || [ "$DIGEST" = "null" ]; then
            echo "FAIL: could not resolve index digest for ${BASE_TAG}" >&2
            exit 1
          fi
          # Confirm this leg's arch is actually in the base index, so a base that
          # regressed to single-arch fails here with a clear message.
          ARCHES=$(curl -s -H "Authorization: Bearer $T" \
            -H 'Accept: application/vnd.oci.image.index.v1+json,application/vnd.docker.distribution.manifest.list.v2+json' \
            "https://ghcr.io/v2/openabdev/openab/manifests/${BASE_TAG}" \
            | jq -r '[.manifests[].platform.architecture]|@csv')
          echo "$ARCHES" | grep -q "${{ matrix.arch }}" || {
            echo "FAIL: base $BASE_TAG has no ${{ matrix.arch }} [arches: $ARCHES]" >&2; exit 1; }
          echo "base_digest=$DIGEST" >> "$GITHUB_OUTPUT"
          echo "Resolved ${BASE_TAG} (index) -> $DIGEST [arches: $ARCHES]"

      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Built into the local daemon (native arch, no emulation) so the smoke
      # test is the gate before anything is published. Same tags/args on the
      # push step below, and buildx serves it from this build's cache, so the
      # bytes tested are the bytes pushed.
      - name: Build the candidate image into the local daemon
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: Dockerfile.nightly
          platforms: linux/${{ matrix.arch }}
          load: true
          push: false
          tags: openab-pty:nightly-candidate
          build-args: |
            OPENAB_BASE_DIGEST=${{ steps.meta.outputs.base_digest }}
            VARIANT=${{ matrix.variant }}
            CLI_VERSION=${{ steps.meta.outputs.cli_version }}
            CLI_SHA256=${{ steps.meta.outputs.cli_sha256 }}
            CLI_REF=${{ steps.meta.outputs.cli_ref }}
          cache-from: type=gha,scope=nightly-${{ matrix.variant }}-${{ matrix.arch }}
          cache-to: type=gha,scope=nightly-${{ matrix.variant }}-${{ matrix.arch }},mode=max
          provenance: false

      # The image.yml contract checks, plus the one this channel exists for: the
      # CLI must be the version resolved this morning. Now runs natively on both
      # arches — the arm64 image is exercised on real arm64 hardware.
      - name: Smoke test the candidate image
        env:
          IMAGE: openab-pty:nightly-candidate
          CLI_VERSION: ${{ steps.meta.outputs.cli_version }}
          CLI_REF: ${{ steps.meta.outputs.cli_ref }}
        run: |
          set -euo pipefail

          # 0. The image really is this leg's arch (not a silent emulation slip).
          GOT_ARCH="$(docker image inspect "$IMAGE" --format '{{.Architecture}}')"
          [ "$GOT_ARCH" = "${{ matrix.arch }}" ] || {
            echo "FAIL: image arch $GOT_ARCH != expected ${{ matrix.arch }}" >&2; exit 1; }
          echo "ok: image architecture is ${{ matrix.arch }}"

          # 1. Refuses to start without an admin credential hash.
          if docker run --rm "$IMAGE" --version >/dev/null 2>&1; then
            echo "FAIL: started with no PTY_ADMIN_HASH" >&2; exit 1
          fi
          echo "ok: refuses to start without PTY_ADMIN_HASH"

          CRED="$(openssl rand -hex 32)"
          HASH="sha256:$(printf '%s' "$CRED" | sha256sum | cut -d' ' -f1)"

          # 2. Refuses an off-loopback bind with no TLS terminator.
          if docker run --rm -e PTY_ADMIN_HASH="$HASH" -e PTY_LISTEN=0.0.0.0:8090 \
               "$IMAGE" >/dev/null 2>&1; then
            echo "FAIL: bound off-loopback with no TLS terminator" >&2; exit 1
          fi
          echo "ok: fail-closed listener guard holds"

          docker run -d --name pty --read-only --tmpfs /tmp \
            --tmpfs /workspace:rw,exec,uid=1000,gid=1000,mode=1777 \
            -e PTY_ADMIN_HASH="$HASH" "$IMAGE" >/dev/null
          for _ in $(seq 1 20); do
            docker exec pty curl -sf -o /dev/null --max-time 2 \
              -H "Authorization: Bearer $CRED" http://127.0.0.1:8090/admin/sessions && break
            sleep 1
          done

          # 3. Admin plane answers the credential and refuses its absence.
          docker exec pty curl -sf --max-time 5 -H "Authorization: Bearer $CRED" \
            http://127.0.0.1:8090/admin/sessions >/dev/null
          echo "ok: admin plane authenticates"
          CODE="$(docker exec pty curl -s -o /dev/null -w '%{http_code}' --max-time 5 \
            http://127.0.0.1:8090/admin/sessions)"
          [ "$CODE" = "401" ] || { echo "FAIL: unauthenticated admin got $CODE, expected 401" >&2; exit 1; }
          echo "ok: unauthenticated admin request gets 401"

          # 4. Sandbox properties hold.
          docker exec pty sh -c '[ "$(id -u)" = 1000 ]' || { echo "FAIL: not uid 1000" >&2; exit 1; }
          docker exec pty sh -c '! command -v sudo >/dev/null' || { echo "FAIL: sudo present" >&2; exit 1; }
          docker exec pty sh -c '! touch /probe 2>/dev/null' || { echo "FAIL: root filesystem writable" >&2; exit 1; }
          echo "ok: uid 1000, no sudo, read-only root filesystem"

          # 5. The channel's promise: the CLI is the latest, and it runs.
          case "${{ matrix.variant }}" in
            antigravity) GOT="$(docker exec pty agy --version)" ;;
            claude)      GOT="$(docker exec pty claude --version)" ;;
            codex)       GOT="$(docker exec pty codex --version)" ;;
            copilot)     GOT="$(docker exec pty copilot --version)" ;;
            cursor)      GOT="$(docker exec pty cursor-agent --version)" ;;
            devin)       GOT="$(docker exec pty devin --version)" ;;
            gemini)      GOT="$(docker exec pty gemini --version)" ;;
            grok)        GOT="$(docker exec pty grok --version)" ;;
            hermes)
              HEAD=$(docker exec pty git -c safe.directory=/usr/local/lib/hermes-agent \
                -C /usr/local/lib/hermes-agent rev-parse HEAD)
              [ "$HEAD" = "$CLI_REF" ] || {
                echo "FAIL: Hermes checkout is $HEAD, expected resolved release commit $CLI_REF" >&2
                exit 1
              }
              GOT="$CLI_VERSION @ $HEAD ($(docker exec pty hermes-acp --version))" ;;
            kiro)        GOT="$(docker exec pty kiro-cli --version)" ;;
            mimocode)    GOT="$(docker exec pty mimo --version)" ;;
            opencode)    GOT="$(docker exec pty opencode --version)" ;;
            pi)          GOT="$(docker exec pty pi --version)" ;;
          esac
          echo "installed CLI reports: $GOT"
          if [ "$CLI_VERSION" != "latest" ]; then
            case "$GOT" in
              *"$CLI_VERSION"*) echo "ok: CLI is the resolved latest ($CLI_VERSION)" ;;
              *) echo "FAIL: expected CLI $CLI_VERSION, image reports: $GOT" >&2; exit 1 ;;
            esac
          fi
          # Recorded for the merge summary — for kiro this is where the version
          # is learned, since the vendor's latest URL carries no version index.
          echo "$GOT" > cli-version-report

          docker rm -f pty >/dev/null

      # Push the exact bytes that passed, by digest only (no tag). The merge job
      # assembles the tags from the per-arch digests. Reuses this leg's cache so
      # nothing is rebuilt between test and push.
      - name: Push the candidate by digest
        id: push
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: Dockerfile.nightly
          platforms: linux/${{ matrix.arch }}
          # Push by digest: no tag written here.
          outputs: type=image,name=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }},push-by-digest=true,name-canonical=true,push=true
          build-args: |
            OPENAB_BASE_DIGEST=${{ steps.meta.outputs.base_digest }}
            VARIANT=${{ matrix.variant }}
            CLI_VERSION=${{ steps.meta.outputs.cli_version }}
            CLI_SHA256=${{ steps.meta.outputs.cli_sha256 }}
            CLI_REF=${{ steps.meta.outputs.cli_ref }}
          cache-from: type=gha,scope=nightly-${{ matrix.variant }}-${{ matrix.arch }}
          provenance: false

      - name: Export the pushed digest
        if: github.event_name != 'pull_request'
        run: |
          set -euo pipefail
          mkdir -p /tmp/digests
          digest="${{ steps.push.outputs.digest }}"
          [ -n "$digest" ] || { echo "FAIL: no digest from push step" >&2; exit 1; }
          # Filename is the digest sans algo prefix; content is the CLI report.
          echo "$digest" > "/tmp/digests/${{ matrix.arch }}"
          echo "arch=${{ matrix.arch }} digest=$digest"

      - name: Upload digest
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: digest-${{ matrix.variant }}-${{ matrix.arch }}
          path: /tmp/digests/${{ matrix.arch }}
          retention-days: 1
          if-no-files-found: error

      - name: Upload CLI report
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: clireport-${{ matrix.variant }}-${{ matrix.arch }}
          path: cli-version-report
          retention-days: 1
          if-no-files-found: warn

  # One merge job per variant: collect the per-arch digests pushed above and
  # bind them under the moving and immutable tags as a single manifest list.
  # `imagetools create` composes existing digests without pulling or rebuilding,
  # so the published manifest references the exact bytes each native runner
  # tested.
  merge:
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        variant: [antigravity, claude, codex, copilot, cursor, devin, gemini,
                  grok, hermes, kiro, mimocode, opencode, pi]
    permissions:
      contents: read
      packages: write
    steps:
      - name: Compute the date tag
        id: meta
        run: echo "date=$(date -u +%Y%m%d)" >> "$GITHUB_OUTPUT"

      - name: Download this variant's arch digests
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          path: /tmp/digests
          pattern: digest-${{ matrix.variant }}-*
          merge-multiple: true

      - name: Download this variant's CLI reports
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          path: /tmp/clireports
          pattern: clireport-${{ matrix.variant }}-*
          merge-multiple: true
        continue-on-error: true

      - uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create and push the manifest list
        env:
          MOVING: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:nightly-${{ matrix.variant }}
          IMMUTABLE: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:nightly-${{ steps.meta.outputs.date }}-${{ github.run_id }}-${{ matrix.variant }}
          REPO: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        run: |
          set -euo pipefail
          # Each file under /tmp/digests holds a sha256:... digest for one arch.
          SRCS=()
          for f in /tmp/digests/*; do
            d="$(cat "$f")"
            case "$d" in
              sha256:*) SRCS+=("${REPO}@${d}") ;;
              *) echo "FAIL: bad digest in $f: $d" >&2; exit 1 ;;
            esac
          done
          [ "${#SRCS[@]}" -ge 1 ] || { echo "FAIL: no arch digests for ${{ matrix.variant }}" >&2; exit 1; }
          echo "Composing ${#SRCS[@]} arch(es): ${SRCS[*]}"

          docker buildx imagetools create -t "$MOVING" -t "$IMMUTABLE" "${SRCS[@]}"

          echo "=== published manifest ==="
          docker buildx imagetools inspect "$MOVING" --format '{{range .Manifest.Manifests}}{{.Platform.OS}}/{{.Platform.Architecture}} {{.Digest}}{{println}}{{end}}'

      - name: Summary
        run: |
          {
            echo "### Nightly image pushed (multi-arch, native builds)"
            echo
            echo '```'
            echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:nightly-${{ matrix.variant }}"
            echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:nightly-${{ steps.meta.outputs.date }}-${{ github.run_id }}-${{ matrix.variant }}"
            echo '```'
            echo
            echo "CLI: $(cat /tmp/clireports/cli-version-report 2>/dev/null || echo 'n/a')"
            echo
            echo "Each arch was built and smoke-tested on its native runner, then bound into the manifest by digest."
            echo
            echo "The image aggregates a third-party agent CLI (the vendor's latest) under its vendor's terms; MIT covers this repository only. See NOTICE."
          } >> "$GITHUB_STEP_SUMMARY"
